# 运行中worker自动提交与租约恢复本地验证

2026-09-05 19:57（上海时间），通过真实HTTP及运行中的独立worker完成 **7项检查，全部PASS**。没有调用测试内的worker方法代替进程，也没有停止或重启API/worker。

演练时进程：

- API：PID80698，`.local/run/worklog-mvp-20260905-1953.jar`，`local,api`。
- worker：PID80802，同一jar，`local,worker`，无HTTP服务。

## HTTP准备的独立测试数据

通过 `/auth/local-login` 建立管理员和员工会话，使用真实CSRF，再通过人员、项目维护与日记录HTTP接口创建：

| 对象 | 实际值 |
|---|---|
| 员工 | `OP18091`，内部ID `180`，仅EMPLOYEE，部门1，MIDDLE |
| 生效日期 / 测试周 | 2026-08-24，上一个自然周的周一 |
| 项目 | ID `182`，名称带“本地worker验证 20260905-195739-bfc56c”，部门1，默认审批人2 |
| 完整日 | 2026-08-24，8小时工作，完整校验通过 |
| 不足日 | 2026-08-25，1小时工作，完整校验不通过（尚缺7小时） |
| 独立现场 | 2026-08-25，同项目现场日，有明确测试事由 |

脚本先验证本地模式、种子管理员身份、健康UP、唯一local worker/API进程，以及测试周月份仍为OPEN；不为测试解锁已封账月份。

## 真实进程执行结果

| 检查 | 结果与证据 |
|---|---|
| 内部入队并由worker执行 | 本地SQL只插入明确标记的AUTO_SUBMIT任务220，payload为人员180/周起日2026-08-24；worker自动领取并完成，attempts=1。 |
| 部分成功 | 8/24工时及8/25现场各生成一个送审条目，审批项83、84，同一周包57；状态均PENDING。 |
| 不完整保留 | 8/25的1小时工时仍DRAFT；任务结果含 `DAY_INCOMPLETE`，没有替员工补造剩余7小时。 |
| 重复payload | 任务221使用同一人员/周payload再次完成，仍为2条审批项、1个包、2条工时内容版本、1条现场内容版本；人工/现场确认费用均0.00。 |
| 过期租约自动恢复 | 任务222预置RUNNING、attempts=1及已过期租约。运行中的worker自动接管并完成，attempts变为2，租约token清空；没有新增审批项、内容版本或费用。 |
| 系统身份审计 | 审计640、641的 `actor_type=SYSTEM`、`actor_id=NULL`、`target_user_id=180`、`job_id=220`，分别追溯工时版本146与现场版本56的送审。 |
| 失败通知 | 通知149、150、151进入员工180本人收件箱，eventType=AUTO_SUBMIT_FAILED、status=LOCAL，正文包含不足日期及原因。LOCAL不是企微发送成功。 |

本次恢复演练模拟“任务已经领取但租约过期”，验证真正运行的worker重新领取持久队列。未人为终止进程，也未声称验证了OS崩溃、跨主机接管或实际企微投递。由于记录仅送审而未由业务审批人确认，费用仍为0.00是正确结果。

## 脚本与证据

```bash
python3 scripts/verify-worker.py
```

- 原始输出：`.local/worker-verification.log`。
- 完整JSON证据：`.local/worker-verification-20260905-195739-bfc56c.json`。
- 脚本：`scripts/verify-worker.py`，仅固定127.0.0.1本地HTTP及allen-worklog本地compose。
- 任务等待上限45秒，通知等待上限20秒，不无限轮询。

HTTP创建业务数据后，SQL写入仅限三个独立AUTO_SUBMIT验证任务；其他SQL均为只读核对。没有重置主库、清理数据库卷或修改其他用户记录。测试员工、项目、送审项和通知均保留并带明确验证标记，可经管理员复核后停用该员工。

## 最终制品复核

2026-09-05 20:11 使用 `worklog-mvp-20260905-2010.jar` 的 API 85333 / worker 85338 再次运行同一脚本，7 项全部通过。此次独立测试人员 OP22475 / 内部 ID 505，任务 268–270，系统审计 1593/1594，本人 LOCAL 通知 474–476。原始结果 `.local/worker-verification-20260905-201124-5a8f1b.json`。没有发送真实企微消息。
