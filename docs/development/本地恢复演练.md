# 本地数据库恢复演练与独立生产迁移校验

2026-09-05 19:53:55–19:54:00（上海时间）完成真实备份恢复，结果 **PASS**。使用运行中的本地 MySQL 8.4，源库为 `worklog`，恢复目标是新建隔离库 `worklog_restore_check_20260905_115355_6d0dc`。原业务库、已有数据库卷、API及worker配置均保留。

## 一致性备份与恢复结果

通过 `mysqldump --single-transaction --quick` 保存全部InnoDB表和数据库结构，并包含 routines/triggers/events 定义。演练期间未进行DDL迁移；正常UI及worker写入可继续。转储禁止扩展INSERT，使每条INSERT对应一条备份记录，进而逐表比较一致性转储中的行数与恢复库行数。

**48张表、2749条记录全部恢复，逐表数量一致。** 下表列出关键业务表：

| 表 | 一致性备份记录数 | 恢复后记录数 | 结果 |
|---|---:|---:|---|
| `app_user` | 133 | 133 | 一致 |
| `day_record` | 70 | 70 | 一致 |
| `time_entry_revision` | 113 | 113 | 一致 |
| `onsite_day_revision` | 46 | 46 | 一致 |
| `approval_item` | 61 | 61 | 一致 |
| `source_record` | 75 | 75 | 一致 |
| `monthly_report_version` | 32 | 32 | 一致 |
| `monthly_snapshot_row` | 106 | 106 | 一致 |
| `audit_event` | 489 | 489 | 一致 |
| `job` | 35 | 35 | 一致 |

正式快照另做内容验证：选择备份中存在的32个正式版本，按稳定ID排序，逐行计算元数据及MySQL规范化JSON的SHA-256。对原库这些不可变版本与恢复库逐行比对后，汇总哈希完全一致：

```text
a31e0d5729d54f37896d76859aa79218fc81fbb12a9a80c6da48f559152f417b
```

因此数量比较不依赖持续写入中的原库当前总数；内容哈希也只比较备份截点已经发布的相同版本，没有把恢复后新增版本混入。

## 保留的产物与复现

- 备份：`.local/backups/worklog-20260905T115355Z.sql.gz`，106488字节。
- 备份SHA-256：`c8409c7996f38ec69ec4b42fe468d540b062e279ce7ffe8bc0948390459bb7d3`。
- 逐表清单及结果：`.local/backups/worklog-20260905T115355Z.sql.verification.json`。
- 原始输出：`.local/recovery-verification.log`。
- 恢复库：`worklog_restore_check_20260905_115355_6d0dc`，保留供后续只读检查。

备份目录权限为700，文件权限为600；产物位于被Git忽略的 `.local/` 内。脚本仅允许种子管理员确认为 `local-admin` 的本地库，不删除备份、原库或恢复库。

```bash
python3 scripts/verify-local-recovery.py
```

每次调用都会产生新命名的隔离恢复库，不能把它用作生产环境的通用恢复命令。

## 不含样例身份的独立迁移验证

独立库 `worklog_migration_check` 从此前V4实际升级至V10，执行 **V5–V10六个迁移** 后再次运行同一迁移命令。结果：

- V1–V10共10条迁移全部 `success=1`；没有V1000/V1001本地样例迁移。
- 重跑显示 `Schema is up to date. No migration necessary.`，两次均 `BUILD SUCCESS`。
- `app_user=0`、`work_item=0`、`day_record=0`，确认不带开发样例身份或业务数据。
- 原始输出：`.runtime/migration-check.log`、`.runtime/migration-repeat-check.log`。

验证使用 `scripts/migrate.sh` 与独立 `deploy/migration-pom.xml`，只读取 `db/migration`。迁移凭据在进程环境注入，没有写入报告。该检查证明正式迁移集合可在无样例业务数据的环境升级并重跑；不是把已有本地种子库的成功启动当作生产迁移验收。

## 演练范围

本次完成数据库备份、隔离恢复、完整记录数核对和正式快照内容核对，未切换应用数据库，未在恢复库启动worker，避免重复业务任务或消息。数据库备份未复制私有导出XLSX文件；这些短期文件需要重新导出。真实生产停机/回切、外部企微/OA重新接入、跨主机故障、对象存储恢复和生产容量没有在本地演练中验证。
